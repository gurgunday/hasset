{
  "name": "hasset",
  "description": "Append unique hashes to assets referenced in your views to aggressively cache them while guaranteeing that clients receive the most recent versions.",
  "author": "Gürgün Dayıoğlu",
  "license": "MIT",
  "version": "0.3.2",
  "type": "module",
  "bin": "./src/bin/cli.js",
  "main": "./src/index.js",
  "exports": "./src/index.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "npm run lint",
    "lint": "eslint . && prettier --check .",
    "lint:fix": "eslint --fix . && prettier --write .",
    "example:build": "node src/bin/cli.js --roots=example/assets --cwds=example/routes/",
    "example": "npm run example:build  && node ./example/server.js"
  },
  "dependencies": {
    "fastq": "^1.17.1",
    "glob": "^10.3.10"
  },
  "devDependencies": {
    "@fastify/pre-commit": "^2.1.0",
    "@fastify/static": "^7.0.1",
    "fastify": "^4.26.1",
    "fastify-html": "^0.3.3",
    "grules": "^0.11.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gurgunday/hasset.git"
  }
}
